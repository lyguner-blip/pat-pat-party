# Pat Pat Party 0.0.2 - 亲密度上升

- Updated the module version to `0.0.2`.
- Added Token HUD calibration for token-top pat offsets, persisted as the `pat-pat-party.patOffset` Token Document flag.
- Replaced the old pat animation with three side-to-side hand animations: gentle, normal, and rough.
- Replaced random chat copy with the fixed localized message `亲密度上升↑` / `Closeness increased ↑`.
- Added socket delegation so non-GM calibration saves can be handled by a single active GM when direct Token flag writes are not permitted.
